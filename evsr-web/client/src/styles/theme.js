import { transparentize } from 'polished';

const theme = {
  primary: '#54a0ff',
  black: '#000',
  grey: '#84817a',
  warning: '#EC644B',
  text: transparentize(0.2, '#000'),
  meta: transparentize(0.4, '#000'),
};

export default theme;
